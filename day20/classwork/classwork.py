# append - ამატებს სიის ბოლოში ელემენტს

my_list = [1,2,3,4]

my_list.append(5)

print(my_list)

# len - სიგრძე(რამდენ ელემენტს შეიცავს სია)

list = ['a','b','c',5]

list_lenght = len(list)\

print(len(list))

# append - ამატებს სიის ბოლოში ელემენტს
# len - სიგრძე(რამდენ ელემენტს შეიცავს სია